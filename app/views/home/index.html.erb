<% if current_admin %> 
  <%= link_to 'Cadastrar cardápio', new_restaurant_menu_path(@restaurant.id) %>

  <% @menus.each do |menu| %>
    <%= link_to menu.name, restaurant_menu_path(@restaurant, menu) %>
  <% end %>
<% end %>